import React, { StyleSheet } from 'react-native';
import colors from '../../colors'

export default StyleSheet.create({
  container: {
  },
  actionIcon: {
    fontSize: 32,
    lineHeight: 27,
    color: "#FFF",
    margin: 5,
  }
});
